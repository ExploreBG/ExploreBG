import React from 'react';

import './home.scss';

interface HomeProps { }

const Home: React.FC<HomeProps> = () => {
    return (
        <main>
            <p>Home page</p>
        </main>
    );
};

export default Home;
