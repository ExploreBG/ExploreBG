export interface ICard {
    id: string
    image: string
    title: string
    description: string
}

export interface IHikeCard {
    id: number
    imageUrl: string
    hikeDate: string
    hikeInfo: string
    location: string
}